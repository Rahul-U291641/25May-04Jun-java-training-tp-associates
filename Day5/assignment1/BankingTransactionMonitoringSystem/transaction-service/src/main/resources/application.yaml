spring:
  application:
    name: transaction-service
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      acks: all
server:
  port: 8081
topics:
  transaction-topic: transaction-json-topic

kind: Application
