spring:
  application:
    name: notification-consumer-service
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-group
      auto-offset-reset: earliest
server:
  port: 8082
topics:
  string-topic: order-string-topic
  json-topic: order-json-topic

kind: Application



